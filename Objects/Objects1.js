// Challeneg area
// Name, age, company

let person={
    name: 'Jaggu',
    age: 28,
    company: 'Bed Bath and Beyond'
}

console.log(`${person.name} is of ${person.age} at ${person.company}`);

person.age +=1

console.log(`${person.name} is of ${person.age} at ${person.company}`);